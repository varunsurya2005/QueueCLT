# QueueCLT
A Python-based CLI tool to manage background jobs with worker processes, retries, exponential backoff, job scheduling, timeout handling, and a Dead Letter Queue, all backed by SQLite for persistence.
